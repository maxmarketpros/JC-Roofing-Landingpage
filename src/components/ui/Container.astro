---
import { cn } from "@/lib/utils";

interface Props {
  class?: string;
  as?: string;
}

const { class: className, as: Tag = "div" } = Astro.props as Props;
---

<Tag class={cn("mx-auto max-w-7xl px-4 sm:px-6 lg:px-8", className)}>
  <slot />
</Tag>
